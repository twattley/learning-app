# agents/

Project-specific agent skill definitions.

Place `.agent.md` or `.md` files here to extend the default agent roster with skills
tailored to this project's stack, patterns, and conventions.

See `CLAUDE.md` for project orientation and `ARCHITECTURE.md` for design decisions
that agents should respect when making changes.
